---
import '@fontsource-variable/manrope';
import '../styles/global.css';
import '../styles/pages.css';
import MobileActionBar from '../components/MobileActionBar.astro';
import fontCyrillic from '@fontsource-variable/manrope/files/manrope-cyrillic-wght-normal.woff2?url';
import fontLatin from '@fontsource-variable/manrope/files/manrope-latin-wght-normal.woff2?url';

interface Props {
  title: string;
  description: string;
  noindex?: boolean;
  image?: string;
  ogType?: 'website' | 'article';
  publishedTime?: string;
}

const { title, description, noindex = false, image = '/og-image.jpg', ogType = 'website', publishedTime } = Astro.props;

const site = Astro.site ?? new URL('https://baikalresearch.ru');
let path = Astro.url.pathname;
if (path !== '/' && !path.endsWith('/')) path += '/';
const canonical = new URL(path, site).href;
const ogImage = new URL(image, site).href;

// Политика безопасности через meta (GitHub Pages не даёт задавать HTTP-заголовки).
// Только в боевой сборке: в режиме разработки она мешает горячей перезагрузке.
// connect-src: адрес функции приёма заявок (*.workers.dev), при своём домене для неё добавить его сюда.
const csp = [
  "default-src 'self'",
  "script-src 'self' https://mc.yandex.ru",
  "style-src 'self'",
  "font-src 'self' data:",
  "img-src 'self' data: https://mc.yandex.ru",
  "connect-src 'self' https://*.workers.dev https://mc.yandex.ru",
  "form-action 'self'",
  "base-uri 'self'",
  "object-src 'none'",
].join('; ');
---
<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  {import.meta.env.PROD && <meta http-equiv="Content-Security-Policy" content={csp} />}
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={canonical} />
  <meta name="robots" content={noindex ? 'noindex, follow' : 'index, follow, max-image-preview:large'} />
  <meta name="theme-color" content="#FFFFFF" />
  <meta name="referrer" content="strict-origin-when-cross-origin" />

  <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
  <link rel="icon" href="/favicon-48.png" type="image/png" sizes="48x48" />
  <link rel="apple-touch-icon" href="/apple-touch-icon.png" />

  <meta property="og:type" content={ogType} />
  {publishedTime && <meta property="article:published_time" content={publishedTime} />}
  <meta property="og:site_name" content="Baikal Research" />
  <meta property="og:locale" content="ru_RU" />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:url" content={canonical} />
  <meta property="og:image" content={ogImage} />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  <meta property="og:image:alt" content="Baikal Research — исследования и бизнес-консалтинг" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={ogImage} />

  <link rel="preload" href={fontCyrillic} as="font" type="font/woff2" crossorigin />
  <link rel="preload" href={fontLatin} as="font" type="font/woff2" crossorigin />

  <slot name="head" />
</head>
<body>
  <slot />
  <MobileActionBar />
  <script is:inline src="/js/metrika.js" defer></script>
  <noscript><div><img src="https://mc.yandex.ru/watch/112962064" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
</body>
</html>
